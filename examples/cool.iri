
proc equals begin
  // Inline implementation of the equals operation
  | mov rcx, 0
  | mov rdx, 1
  | pop rax
  | pop rbx 
  | cmp rax, rbx
  | cmove rcx, rdx
  | push rcx
end


0
loop begin
  1 + dupl dupl
  print
  100 equals if
    then begin 0 exit end
    else begin 0 drop end
end